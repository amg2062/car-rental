import { environment } from "../environment/environment";;

export const urls = {
    fetchCars: `${environment.baseurl}/api/Cars`,
    createReservation: `${environment.baseurl}/api/Cars/create-reservation`
  };